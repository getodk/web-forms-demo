# Web Forms demo

Basic vue app used for demonstrating @getodk/web-forms

## Development

- To build everything, use `npm run build`
- To build and run the code in development mode, use `npm run dev`
- To deploy a preview of the dist code, use `npm run preview`
- To run the smoketest, first start the app `npm run dev` and then use `npm run test`
